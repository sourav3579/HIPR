import { PremiumRangeFormatPipe } from './premium-range-format.pipe';

describe('PremiumRangeFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new PremiumRangeFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
